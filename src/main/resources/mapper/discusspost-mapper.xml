<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wtz.community.dao.DiscussPostMapper">   <!--配置为哪个mapper文件-->
        <sql id="selectFields">
            id, user_id, title, content, type, status, create_time, comment_count, score
        </sql>
        <select id="selectDiscussPosts" resultType="com.wtz.community.entity.DiscussPost">
            select <include refid="selectFields"></include>
            from discuss_post
            where status != 2   <!--拉黑帖子不展示-->
            <if test="userId!=0">
                and user_id = #{userId}
            </if>
            order by type desc, create_time desc       <!--帖子排序按类型、时间-->
            limit #{offset}, #{limit}
        </select>
        <select id="selectDiscussPostRows" resultType="int">
            select count(id)
            from discuss_post
            where status != 2   <!--拉黑帖子不展示-->
            <if test="userId!=0">
                and user_id = #{userId}
            </if>
        </select>
</mapper>