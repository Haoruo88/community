# ServerProperties
server.port=8080
server.servlet.context-path=/community  
# ThymeleafProperties
spring.thymeleaf.cache=false
# DataSourceProperties
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/community?characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=010828
    #数据库连接池 最大连接数 最大保留连接数 连接关闭等待最大时间
spring.datasource.type=com.zaxxer.hikari.HikariDataSource
spring.datasource.hikari.maximum-pool-size=15
spring.datasource.hikari.minimum-idle=5 
spring.datasource.hikari.idle-timeout=30000

# MybatisProperties 映射配置文件
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.wtz.community.entity
mybatis.configuration.useGeneratedKeys=true
    # 使驼峰命名方式（属性）和下划线命名方式（数据表中的字段）能匹配
mybatis.configuration.mapUnderscoreToCamelCase=true

#   该包日志级别调为debug:便于sql语句的调试
logging.level.com.wtz.community=debug

#   MailProperties
spring.mail.host=smtp.sina.com
spring.mail.port=465        
spring.mail.username=community_wtz@sina.com
spring.mail.password=9bdb433f14906c4c
spring.mail.protocol=smtps
spring.mail.properties.mail.smtp.ssl.enable=true